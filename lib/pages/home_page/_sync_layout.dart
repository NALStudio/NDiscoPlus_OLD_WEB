import 'package:flutter/material.dart';

class SyncLayout extends StatelessWidget {
  const SyncLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("SYNC LAYOUT"),
    );
  }
}
